var one = 64;
var two = "656302";
var three = false;
var four = 64.55;
var five = "Howdy!";
var six;

// the value type for one (number)
console.log(typeof one);

// the value type for two (string)
console.log(typeof two);

// the value type for three (boolean)
console.log(typeof three);

// the value type for four (number)
console.log(typeof four);

// the value type for five (string)
console.log(typeof five);

// the value type for six (undefined)
console.log(typeof six);

four = "Hello!";
five = false;
six = 23;

// the new value type for four (string)
console.log(typeof four);

// the new value type for five (boolean)
console.log(typeof five);

// the new value type for six (number)
console.log(typeof six);