// TODO: What will the following line of code log?
// the entire window object (everything)
console.log(window);

// TODO: What will the following line of code log?
// the entire HTML document within the window (including DOCTYPE)
console.log(window.document);

// TODO: What will the following line of code log?
// the entire <html> object
console.log(document.documentElement);

// TODO: What will the following line of code log?
// the document’s <head> object
console.log(document.head);
